# Catastrophic shell command denylist (POSIX-ERE & Windows patterns)
# Sourced from davidondrej/skills ops-and-setup/global-agent-guardrails

# Destructive file removal
rm\s+(-[a-zA-Z]*r[a-zA-Z]*f|-[a-zA-Z]*f[a-zA-Z]*r)\s+(/|~|\$HOME|\.\.)(\s|$)
rm\s+(-[a-zA-Z]*r[a-zA-Z]*f|-[a-zA-Z]*f[a-zA-Z]*r)\s+\*
sudo\s+rm\s+(-[a-zA-Z]*r[a-zA-Z]*f|-[a-zA-Z]*f[a-zA-Z]*r)
del\s+/[sS]\s+/[qQ]\s+[cC]:\\

# Raw device writes & disk formatting
mkfs(\.[a-z0-9]+)?\s+
dd\s+if=.*of=/dev/
format\s+[a-zA-Z]:(\s|$)

# Remote shell execution / untrusted pipelining
curl\s+.*\|\s*(ba)?sh(\s|$)
wget\s+.*\|\s*(ba)?sh(\s|$)
fetch\s+.*\|\s*(ba)?sh(\s|$)

# Fork bombs & denial of service
:\(\)\s*\{\s*:\s*\|\s*:\s*&\s*\}\s*;\s*:
while\s+true;\s*do\s+.*&\s*done

# Catastrophic git destruction
git\s+push\s+.*--force
git\s+push\s+.*-f(\s|$)
git\s+push\s+.*:([a-zA-Z0-9_\-]+)
git\s+branch\s+(-D|-d)\s+(main|master|prod|production)
git\s+reset\s+--hard\s+HEAD~[0-9]{2,}

# Overly permissive root permission tampering
chmod\s+(-R\s+)?777\s+(/|~|\$HOME)(\s|$)

# Credential store tampering & secret extraction
security\s+dump-keychain
vaultcmd(\.exe)?(\s|$)
mimikatz(\.exe)?(\s|$)
cat\s+.*\.env(\s*\||\s*>|\s*>>|\s+https?://)
type\s+.*\.env(\s*\||\s*>|\s*>>|\s+https?://)
(Get-Content|gc)\s+.*\.env(\s*\||\s*>|\s*>>|\s+https?://)

# Remote exfiltration via webhooks
curl\s+.*-(d|F|X\s*POST).*(webhook|requestbin|pastebin|ngrok|pipedream|hookdeck)
(Invoke-WebRequest|iwr|Invoke-RestMethod|irm).*-(Method\s+Post|Body).*(webhook|requestbin|pastebin|ngrok|pipedream|hookdeck)

# System and registry destruction
reg\s+delete\s+HKLM
bcdedit\s+/delete
